# Классификация обращений NYC 311 с использованием KerasTuner и лог-нормализованных весов классов

## Описание проекта
Данный проект представляет собой полный исследовательский конвейер по построению модели классификации текстовых обращений службы 311 Нью-Йорка.  
В работе совмещены:

- загрузка данных через NYC Open Data API;
- очистка и нормализация текстовых полей;
- агрегация редких классов (с выделением категории OTHER);
- лог-нормализованные веса классов;
- автоматический гиперпараметрический поиск с помощью **KerasTuner (Hyperband)**;
- обучение и оценка модели;
- сохранение готовой модели.

Полученная модель достигает **точности 99.89%** на тестовой выборке.

---

## 📊 Используемый датасет
Источник данных:  
NYC Open Data — 311 Service Requests (`erm2-nwe9`)

Используемые поля:

- `descriptor`
- `complaint_type`
- `borough`
- `location_type`

Для модели создаётся комбинированное текстовое поле из этих атрибутов.

---

## 🔍 Этапы работы

### 1. Загрузка и очистка данных
- удаление строк с пустыми обязательными полями;
- заполнение пропусков в `borough` и `location_type`;
- формирование объединённого текстового поля.

### 2. Формирование топ-N классов
- выбираем `top_n = 10` наиболее частых типов обращений;
- все остальные объединяем в класс **OTHER**.

### 3. Лог-нормализованные веса классов
Используется следующая схема:

1. логарифмирование количества обращений: `log1p(count)`
2. min-max нормализация логов;
3. инверсия (редкие классы получают больший вес);
4. нижний порог веса `0.2`;
5. выравнивание среднего веса до `1.0`.

Это снижает дисбаланс классов и улучшает устойчивость модели.

### 4. Токенизация и паддинг
- словарь — до 30 000 слов;
- длина последовательности — 80 токенов.

### 5. Модель и гиперпараметрический поиск
KerasTuner подбирает:

- размер эмбеддинга,
- архитектуру (`lstm` или `conv`),
- количество фильтров / units,
- dropout,
- размер плотного слоя,
- learning rate.

Алгоритм поиска: **Hyperband**.

### 6. Оценка лучшей модели
Выводится:

- accuracy,
- classification report,
- матрица ошибок.

### 7. Сохранение модели
Файл модели:

improved_311_model_with_class_weights.keras

## 📈 Результаты

### Лучшие гиперпараметры
emb_dim = 128
arch = conv
filters = 64
kernel = 3
dropout = 0.4
dense_units = 64
lr ≈ 0.00104

### Итоговая точность

Test accuracy: 0.9989